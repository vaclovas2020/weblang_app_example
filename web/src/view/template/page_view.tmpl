<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{{define "model.title"}}{{.}}{{end}}</title>
    </head>
    <body>
        <div class="container">
            {{define "model.content"}}{{.}}{{end}}
        </div>
    </body>
</html>